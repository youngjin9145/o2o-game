name: seogwipo_investment_game
description: 제주 유네스코 세계유산 투자 게임 앱

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # State Management
  flutter_bloc: ^8.1.3
  bloc: ^8.1.2
  
  # Navigation
  go_router: ^12.1.3
  
  # Dependency Injection
  get_it: ^7.6.4
  injectable: ^2.3.2
  
  # Charts
  fl_chart: ^0.66.0
  
  # HTTP & Data
  dio: ^5.3.2
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  
  # UI
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  
  # Utils
  equatable: ^2.0.5
  dartz: ^0.10.1
  shared_preferences: ^2.2.2
  crypto: ^3.0.3
  flutter_dotenv: ^5.1.0
  uuid: ^4.5.1
  
  # Database
  supabase_flutter: ^2.5.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  
  # Code Generation
  build_runner: ^2.4.7
  freezed: ^2.4.6
  json_serializable: ^6.7.1
  injectable_generator: ^2.4.1

flutter:
  uses-material-design: true
  # assets:
  #   - .env  # Removed for web deployment - using hardcoded values in main.dart 